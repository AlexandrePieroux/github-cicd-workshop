name: Protected envs and stages

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  hello-greet-js:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: ./.github/actions/custom.yaml
        with:
          who-to-greet: 'Mona the Octocat'
